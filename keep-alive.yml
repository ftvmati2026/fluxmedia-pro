name: Keep Render Awake

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Visitar la pagina para que no se duerma
        run: curl -sSf https://fluxmedia-pro.onrender.com/ || true
